# --- Database (Postgres) ---
# NOTE: '!' must be URL-encoded as %21 in URLs.
DATABASE_URL=postgresql+psycopg://isro_user:StrongPass%21admin123@127.0.0.1:5432/isro_proposals

# --- (Fallback) SQLite ---
# To switch back temporarily, comment the Postgres line above and uncomment below.
# DATABASE_URL=sqlite:///./storage/app.db

# --- File storage (relative to backend root C:\Projects\backend) ---
UPLOAD_DIR=storage/uploads

# --- OCR (optional; uncomment ONLY if installed) ---
# If you installed Tesseract (for image OCR), set this:
# TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe
# If you installed Poppler (for pdf2image), set this (adjust version/path):
# POPPLER_PATH=C:\Program Files\poppler-24.08.0\Library\bin

# --- Ollama (optional; leave as-is if not using yet) ---
OLLAMA_URL=http://127.0.0.1:11434
OLLAMA_MODEL=mistral

# --- CORS (Frontend origin) ---
# Change to match your frontend port exactly (e.g., 3000 or 3001).
FRONTEND_ORIGIN=http://127.0.0.1:3000
